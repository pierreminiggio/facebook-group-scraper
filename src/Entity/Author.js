export default class Author {
    
    /** @type {string} */
    name

    /** @type {string} */
    link

    /**
     * @param {string} name 
     * @param {string} link 
     */
    constructor(name, link) {
        this.name = name
        this.link = link
    }
}
